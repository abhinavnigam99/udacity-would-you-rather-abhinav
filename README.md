This project was bootstrapped with [Create React App]

Steps : 

npm i
yarn start/npm start

I tried using few libraries and components but could not get them working as intended even after using bootstrap and reactstrap. So the UI even though much code is added is horrible.

Will try to get them done in future.